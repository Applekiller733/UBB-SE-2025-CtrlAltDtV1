<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SocialStuff.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SocialStuff"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SocialStuff">

    <Grid x:Name="MainGrid">
        <!-- Define Two Columns: Left Navigation & Right Content -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#1b1a26" Padding="10">
            <!-- Top Navigation Bar -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Padding="10">
                <Button Content="Chat" />
                <Button Content="Feed"/>
                <Button Content="Friends" />
                <Button Content="Notifications" />
            </StackPanel>

            <!-- Search Box with FontIcon -->
            <Grid Margin="0,10,0,10">
                <TextBox x:Name="SearchBox" 
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     PlaceholderText="Search chats..."
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     IsSpellCheckEnabled="True"
                     SelectionHighlightColor="Green"
                     Padding="5"
                     Width="260"/>
                <FontIcon Glyph="&#xE71E;" 
                      VerticalAlignment="Center"
                      HorizontalAlignment="Right"
                      Margin="0,0,10,0"/>
            </Grid>

            <StackPanel Orientation="Horizontal" Spacing="10" Margin="13, 0, 0, 0">
                <TextBlock Text="Chats" FontSize="24" FontWeight="Bold" />
                <Button Content="+" />
            </StackPanel>


            <Grid Width="300" Height="400">
                <!-- ListView for displaying chats -->
                <ListView x:Name="ChatList"
                    ItemsSource="{Binding ChatList}"
                    SelectionMode="Single"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    Visibility="{Binding ChatList.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=HasItems}"
                    SelectionChanged="ChatList_SelectionChanged"/>

                <!-- TextBlock for empty state -->
                <TextBlock Text="You have no chats!"
                    FontSize="18"
                    Foreground="Gray"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding ChatList.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=NoItems}"/>   
            </Grid>
            
        </StackPanel>

        <Frame x:Name="ContentFrame" Grid.Column="1"/>

    </Grid>
</Window>
