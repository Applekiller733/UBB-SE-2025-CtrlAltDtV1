<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SocialStuff.View.ChatListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SocialStuff.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ChatListView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Pink">
        </Border>

        <Border Grid.Row="1" Background="Black">
        </Border>

        <Border Grid.Row="2" Background="Pink">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <Button Width="40" Height="40" Margin="10,0"
                        Background="#614E52" BorderBrush="Black" BorderThickness="1"
                        VerticalAlignment="Center">
                    <Image Source="https://www.svgrepo.com/show/309379/camera-add.svg" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                </Button>
                <TextBox 
                    AutomationProperties.Name="multi-line TextBox"
                    TextWrapping="Wrap" 
                    AcceptsReturn="True" 
                    IsSpellCheckEnabled="True"
                    SelectionHighlightColor="Green" 
                    MinWidth="400"
                    VerticalAlignment="Center" 
                    Height="Auto"
                    PlaceholderText="Type a message"
                    Background="#614E52"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
